query getPostsByTags($tags: [ID], $perPage: Int) {
  Posts: getPostsByTags(tags: $tags, perPage: $perPage) {
    id
    categories
    featured_media
    author
    date
    title
    content
    slug
    excerpt
  }
}
